[project]
name = "dev-alchemy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "redis>=6.2.0",
]

[tool.uv.workspace]
members = ["python_ulid_benchmark"]
